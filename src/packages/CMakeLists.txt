add_subdirectory(opendcc.ui.code_editor)
add_subdirectory(opendcc.ui.script_editor)

add_subdirectory(opendcc.usd_editor.common_cmds)
add_subdirectory(opendcc.usd_editor.common_tools)

if(DCC_BUILD_ANIM_ENGINE)
    add_subdirectory(opendcc.vendor.animx)
    add_subdirectory(opendcc.anim_engine.schema)
    add_subdirectory(opendcc.anim_engine.curve)
    add_subdirectory(opendcc.anim_engine.core)
    add_subdirectory(opendcc.anim_engine.ui.graph_editor)
    add_subdirectory(opendcc.anim_engine)
endif()
if(DCC_BUILD_BULLET_PHYSICS)
    add_subdirectory(opendcc.usd_editor.bullet_physics)
endif()
if(DCC_BUILD_EXPRESSIONS_ENGINE)
    add_subdirectory(opendcc.expression)
endif()
if(DCC_PACKAGE_OPENDCC_USD_EDITOR_UV_EDITOR)
    add_subdirectory(opendcc.usd_editor.uv_editor)
endif()
if(DCC_PACKAGE_OPENDCC_USD_EDITOR_SCULPT_TOOL)
    add_subdirectory(opendcc.usd_editor.sculpt_tool)
endif()
if(DCC_PACKAGE_OPENDCC_USD_EDITOR_POINT_INSTANCER_TOOL)
    add_subdirectory(opendcc.usd_editor.point_instancer_tool)
endif()
if(DCC_PACKAGE_OPENDCC_USD_EDITOR_PAINT_PRIMVAR_TOOL)
    add_subdirectory(opendcc.usd_editor.paint_primvar_tool)
endif()
if(DCC_PACKAGE_OPENDCC_USD_EDITOR_TEXTURE_PAINT_TOOL)
    add_subdirectory(opendcc.usd_editor.texture_paint)
endif()
if(DCC_PACKAGE_OPENDCC_USD_EDITOR_BEZIER_TOOL)
    add_subdirectory(opendcc.usd_editor.bezier_tool)
endif()
if(DCC_PACKAGE_OPENDCC_USD_EDITOR_LIVE_SHARE)
    add_subdirectory(opendcc.usd_editor.live_share)
endif()
if(DCC_NODE_EDITOR)
    add_subdirectory(opendcc.external.graphviz)
    add_subdirectory(opendcc.ui.node_editor)
    add_subdirectory(opendcc.usd_editor.usd_node_editor)
    add_subdirectory(opendcc.usd_editor.material_editor)
endif()

if(DCC_BUILD_HYDRA_OP)
    add_subdirectory(opendcc.hydra_op.schema)
    add_subdirectory(opendcc.hydra_op.translator)
    if(DCC_NODE_EDITOR)
        add_subdirectory(opendcc.hydra_op.ui.node_editor)
    endif()
    add_subdirectory(opendcc.hydra_op.render)
    add_subdirectory(opendcc.hydra_op)
    if(DCC_PACKAGE_OPENDCC_HYDRA_OP_SCENE_GRAPH AND USD_VERSION VERSION_GREATER_EQUAL 0.25.02)
        add_subdirectory(opendcc.hydra_op.ui.scene_graph)
    endif()
    if(DCC_PACKAGE_OPENDCC_HYDRA_OP_ATTRIBUTE_VIEW AND USD_VERSION VERSION_GREATER_EQUAL 0.25.02)
        add_subdirectory(opendcc.hydra_op.ui.attribute_view)
    endif()
    if(USD_VERSION VERSION_GREATER_EQUAL 0.23.05)
        add_subdirectory(opendcc.hydra_op.ui.scene_browser)
    endif()
endif()

if(USD_VERSION VERSION_GREATER_EQUAL 0.25.02)
    add_subdirectory(opendcc.usd_editor.expression_variables_toolbar)
endif()
if(USD_VERSION VERSION_GREATER_EQUAL 0.22.05 AND DCC_PACKAGE_OPENDCC_USD_EDITOR_CANVAS)
    add_subdirectory(opendcc.usd_editor.canvas)
    add_subdirectory(opendcc.usd_editor.canvas.schema)
endif()
add_subdirectory(opendcc.usd_editor.scene_indices)
